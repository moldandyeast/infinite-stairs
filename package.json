{
  "name": "infinite-stairs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/",
    "check": "node --check public/parade.js && node --check public/move/move.js",
    "test:browser": "node test/browser.mjs"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "wrangler": "^4.110.0"
  }
}
